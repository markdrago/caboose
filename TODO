value:
- make % of code in a given subset of dirs stat work w/ config
  X perhaps add ability to have a stat which is not written to index
  - then have a stat which reads from already-written results files

tech debt:
- grep for TODO
